# Dependencies for pytest (CI and local: pip install -r requirements-test.txt)
pytest>=8
# Panel/coordinator tests import Home Assistant types and helpers
homeassistant>=2024.12.0
# Explicit pins align with manifest / common HA stack
aiohttp>=3.9.0
voluptuous>=0.13.0
pydantic>=2.8.0
